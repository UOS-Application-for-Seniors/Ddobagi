package com.example.ddobagi.Class;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.FrameLayout;
import androidx.annotation.Nullable;

public class PlayResultCardLayout extends FrameLayout {
    public PlayResultCardLayout(Context context) {
        super(context);
        init(context);
    }

    public PlayResultCardLayout(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
        init(context);
    }

    private void init(Context context){

    }
}